# Rust 待优化项目

本文记录 Rust 实现当前需要后续统一处理的架构、设计和一致性问题。配置文件分离已经在本轮执行；配置写入一致性、Python/PyO3 等问题仍按计划先记录，不在本轮修复。

## 本次准备做的工作

1. 将 Rust 代码审查中发现的不合理架构和设计问题落成文档。
2. 查清 Rust 与 Python 两侧当前配置文件路径、默认目录和配置职责划分。
3. 将 Rust 配置拆成 `mcp.json` 与 `config.toml` 两个文件。
4. 将配置写入一致性问题作为后续统一治理项记录，现版本先抛出问题，不立即修复。
5. 将 Python/PyO3 绑定相关问题作为后续统一治理项记录，现版本先抛出问题，不立即修复。

## 配置文件现状

### Rust 侧配置路径

Rust 配置路径由 `rust/components/config/src/resolver.rs` 和 `rust/components/config/src/lib.rs` 中的 `ConfigManager` 决定。

`mcp.json` 的默认查找顺序如下：

1. 当前工作目录下的 `mcp.json`。
2. 用户目录下的 `~/.mcpstore/mcp.json`。
3. 系统配置目录下的 `mcp.json`：
   - macOS：`/Library/Application Support/mcpstore/mcp.json`
   - Windows：`C:\ProgramData\mcpstore\mcp.json`
   - Linux/Unix：`/etc/mcpstore/mcp.json`

如果以上路径都不存在，新建配置时默认落到 `~/.mcpstore/mcp.json`，避免在当前工作目录下隐式生成新的 `mcp.json`。

`config.toml` 与选中的 `mcp.json` 同目录。例如：

- 默认用户配置：`~/.mcpstore/mcp.json` 与 `~/.mcpstore/config.toml`。
- 显式传入 `--config-path /tmp/mcp.json`：对应 `/tmp/mcp.json` 与 `/tmp/config.toml`。

CLI 和 UI 也支持通过 `--config-path` 显式指定 MCP 配置文件路径。显式传入时，Rust 使用该路径创建 `ConfigManager`，并从同目录解析 `config.toml`。

### Rust 侧配置内容是否分离

Rust 当前已经拆成两个文件：

`mcp.json` 承载 MCP 相关内容：

- `mcpServers`：MCP 服务定义。
- `agents`：Agent 与服务授权关系。

`config.toml` 承载 MCPStore 全局运行配置：

- `version`、`description`、`created_by`、`created_at` 等元信息。
- `[cache]`：缓存后端配置，例如 `backend`、`redis_url`、`namespace`。

运行态数据不写入 `mcp.json` 或 `config.toml`，而是写入内存或 Redis 的 cache layer：

- `entity`
- `relations`
- `state`
- `event`

结论：Rust 侧已与 Python 侧保持“JSON 管 MCP 服务，TOML 管全局配置”的分文件结构。Agent 与服务授权关系当前仍保留在 `mcp.json`，因为它属于 MCP 服务可见性关系，需要跨 CLI 进程持久化。

### Python 侧配置路径

Python 侧默认路径由 `src/mcpstore/config/path_utils.py` 定义：

1. MCP 服务配置：`~/.mcpstore/mcp.json`
2. 全局非敏感配置：`~/.mcpstore/config.toml`
3. 用户数据目录：`~/.mcpstore`

`src/mcpstore/config/json_config.py` 中的 `MCPConfig` 默认使用 `~/.mcpstore/mcp.json`。`src/mcpstore/config/toml_config.py` 负责初始化和读取 `~/.mcpstore/config.toml`。

### Python 侧配置内容是否分离

Python 侧已经做了职责分离：

- `mcp.json`：MCP 服务配置，核心字段是 `mcpServers`。
- `config.toml`：全局非敏感配置，例如 server、health_check、monitoring、standalone 等；已删除无消费者的 `content_update` 配置面。
- cache 配置当前主要由代码参数传入，例如 `setup_store(cache=...)`，不是 `config.toml` 的主要管理对象。

`src/mcpstore/core/configuration/unified_config.py` 也把配置类型拆成：

- `STANDALONE`：来自 `config.toml`。
- `MCP_SERVICES`：来自 `mcp.json`。
- `CLIENT_SERVICES`、`AGENT_CLIENTS`：当前单一数据源架构下已返回空映射或废弃。

结论：Python 侧 MCP 服务内容和全局配置已经分文件管理；Rust 侧本轮已按同一方向完成分离。

## 高优先级待优化项

### 1. 配置读取错误不能被静默吞掉

位置：

- `rust/components/config/src/lib.rs`
- `rust/components/core/src/store.rs`

问题：

Rust 的 `load_or_default()` 仍会把 `mcp.json` 不存在、JSON 解析失败、IO 错误全部转换成默认空配置。后续 `add_service`、`assign`、`update` 等操作可能在错误配置基础上继续写入，存在覆盖用户原配置的风险。

本轮新增的 `config.toml` 读取没有沿用这个静默策略：文件不存在时使用默认全局配置，TOML 解析失败或 IO 错误会向上返回。

建议：

后续统一治理时，区分以下情况：

- 文件不存在：只有在明确初始化或允许创建时才生成默认配置。
- JSON 解析失败：直接返回错误。
- IO/权限错误：直接返回错误。
- 写入前做显式校验，禁止用空默认配置掩盖问题。

当前处理策略：

现版本先记录问题，不立即修改。后续与配置一致性治理一起解决。

### 2. 服务写入缺少事务边界

位置：

- `rust/components/core/src/store.rs`
- `rust/apps/cli/src/commands/mcp.rs`

问题：

`add_service` 当前先写 registry、connection pool、cache，最后才写 `mcp.json`。如果最后配置保存失败，内存态或 Redis 可能已经认为服务存在，但配置文件没有成功持久化。

`update` 当前先 `remove_service`，并且忽略删除错误，然后再 `add_service`。如果新增失败，旧服务可能已经被移除。

建议：

后续统一治理时，明确 durable source 和 runtime state 的写入顺序：

- 先校验配置。
- 再以一个明确的服务写入流程提交 durable source。
- 最后更新 registry、pool、cache。
- 对失败路径做可追踪错误返回，避免静默忽略。

当前处理策略：

现版本先抛出问题，不立即解决。后续统一做配置写入一致性设计。

### 3. 连接成功后工具发现失败被伪装成空工具列表

位置：

- `rust/components/core/src/store.rs`
- `rust/apps/cli/src/commands/mcp.rs`

问题：

连接服务后调用 `list_tools` 时使用 `unwrap_or_default()`。如果 MCP 服务工具列表拉取失败，会被当作空工具列表处理，服务仍会被标记为 connected/healthy。

风险：

- 协议错误、认证错误、服务端错误都可能被隐藏。
- 用户看到服务已连接但工具为空，排查困难。
- 后续 cache 中的服务状态不可信。

建议：

把工具发现视为连接流程的一部分。工具发现失败时应返回错误，并写入明确的 degraded/error 状态。

### 4. ConnectionPool 锁粒度过大

位置：

- `rust/components/transport/src/client.rs`

问题：

`ConnectionPool` 在持有全局 `RwLock<HashMap<...>>` 时执行 `connect`、`list_tools`、`call_tool` 等网络 await 操作。慢连接或慢工具调用会阻塞其它服务的连接池操作。

建议：

后续把连接池结构调整为：

- 全局 map：`RwLock<HashMap<String, Arc<Mutex<McpConnection>>>>`
- 查找连接时短暂持有 map 锁。
- 网络操作只持有单个连接的锁。

## 中优先级待优化项

### 5. Redis 枚举使用 KEYS

位置：

- `rust/components/cache/src/lib.rs`

问题：

Redis 后端在 `collections()` 和 `keys()` 中使用 `KEYS`。数据量变大时会阻塞 Redis。

建议：

使用 `SCAN` 或维护显式集合索引来获取集合和 key 列表。

### 6. Agent scope 可能重复写入

位置：

- `rust/components/registry/src/scope.rs`

问题：

`add_to_agent_scope` 直接向 `Vec` push，不做去重。重复加载配置或重复授权时，Agent scope 可能出现重复服务。

建议：

内部用 `HashSet` 或有序集合保证唯一性。输出时按需要排序或保持插入序。

### 7. 配置校验模型不完整

位置：

- `rust/components/config/src/lib.rs`
- `rust/components/config/src/validator.rs`
- `rust/components/config/src/models.rs`

问题：

Rust 配置校验当前只检查服务是否有 `url` 或 `command`。非法 `transport` 可能在转换过程中变成 `None`，没有被明确报错。

建议：

后续把 transport 改成强类型 enum，或在 validator 中保留原始字符串并明确报错。

### 8. 文档状态与仓库实际内容不一致

位置：

- `rust/IMPLEMENTATION_STATUS.md`

问题：

文档中记录了 `mcpstore-fixture-server`，但当前 workspace 中没有该 crate。文档还记录了若干已验证命令，后续需要和实际仓库同步。

建议：

补齐 fixture crate，或更新文档删除不可执行内容。

## Python/PyO3 待处理项

### 9. PyO3 绑定尚不适合作为生产边界

位置：

- `rust/bindings/python/src/cache.rs`
- `rust/bindings/python/src/registry.rs`
- `rust/bindings/python/src/core_store.rs`

问题：

- 当前 PyO3 层大量使用 `block_on`。
- Python API 暴露 JSON 字符串，而不是自然的 Python dict/list。
- `ConfigManager` 仍是占位实现。
- 当前已经接入 `mcpstore_core`，但同步/异步边界、Python 原生对象映射与异常设计仍需继续治理。

建议：

后续统一设计 Python/Rust 边界：

- 明确 sync API 与 async API。
- 使用 Python 原生对象做输入输出。
- 把错误类型映射成清晰的 Python 异常。
- 再决定是否让 Python SDK 选择 Rust core。

当前处理策略：

现版本先记录，不立即处理。

## 静态检查与测试现状

已验证：

```bash
CARGO_HOME=/tmp/cargo-home-mcpstore cargo test --workspace
```

结果：

- `mcpstore_cache`：8 个测试通过。
- `mcpstore_cli`：18 个测试通过。
- `mcpstore_config`：3 个测试通过。
- `mcpstore_core`：5 个测试通过。
- `mcpstore_events`：3 个测试通过。
- `mcpstore_registry`：1 个测试通过。
- `mcpstore_python`、`mcpstore_transport`：当前无单元测试。

未通过：

```bash
CARGO_HOME=/tmp/cargo-home-mcpstore cargo clippy --workspace --all-targets -- -D warnings
```

当前阻塞点：

- `components/events/src/bus.rs`：`SubscriberMap::new()` 缺少 `Default` 实现。
- `components/events/src/lib.rs`：`Ok(handler.handle(...).await)` 触发 `unit_arg` lint。

## 建议处理顺序

1. 统一配置读取/写入错误策略，禁止配置错误静默变默认值。
2. 设计服务写入一致性流程，明确 `mcp.json`、registry、pool、cache 的提交顺序。
3. 修正连接流程中的工具发现错误处理。
4. 调整 `ConnectionPool` 锁粒度。
5. 替换 Redis `KEYS`。
6. Agent scope 去重。
7. 补齐配置校验。
8. 同步 Rust 状态文档和 fixture。
9. 统一设计 Python/PyO3 边界。
